# Bảo mật E2E
HTTP là truyền tin bị MITM attack. HTTPs cản được vì thông tin truyền đi bị mã hóa khi thiết lập SSL connection với server. Nhưng như v server vẫn nhìn thấy nội dung data và biết thông tin nhạy cảm của user
Giao thức E2E giải quyết vấn đề: 2 users trao đổi pubkey qua server, mỗi user dùng pubkey của người kia để mã hoá trước khi gửi tới server, server chỉ forward tin tới đích. Private key được lưu local ở máy của user, quên pass sẽ kbh lấy lại tin nhắn được nữa.
=> Hacker tấn công server cũng k biết được message là gì, user phải nhớ mật khẩu mới xem được lịch sử tin nhắn.



# Bulk API Pattern
Tạo 1 endpoint /$batch cho user gửi 1 request chứa nhiều request khác. 

-> Odata batch dùng mutipart với từng phần là application/http => nên dùng thay vì JSON batch
Request:
POST /$batch
Content-Type: multipart/mixed; boundary=batch_123
Body:
--batch_123
Content-Type: text/plain

Hello

--batch_123
Content-Type: image/png

(binary data)

--batch_123
Content-Type: multipart/mixed; boundary=changeset_456

--changeset_456
Content-Type: application/http
Content-Transfer-Encoding: binary

POST /users HTTP/1.1
Content-Type: application/json
{
  "name": "Alice"
}
--changeset_456
Content-Type: application/http
Content-Transfer-Encoding: binary

DELETE /inventory/1 HTTP/1.1
--changeset_456--

--batch_123--

Server và client phải tự xử lý parse request và response thủ công.
changeset không chứa GET, chỉ chứa các action thay đổi data và server cần đảm bảo 1 changeset là 1 tx, 1 lệnh failed thì rollback cả changeset đó.
batch
  ├── GET
  ├── changeset A
      ├── POST
      ├── PATCH
      └── DELETE
  ├── GET
  └── changeset B
      ├── POST
      ├── PATCH
      └── DELETE
Kiểu Content-Type: multipart/mixed; boundary=abc123 thì multipart/mixed dùng nhiều kiểu dữ liệu, boundary là phần ngăn cách trong multipart. Còn có multipart/form-data chuyên dùng cho HTML form, VD <form enctype="multipart/form-data">
K có yêu cầu về chạy song song, có thể implement gom các hàm chung thành 1, chạy song song từng phần GET và changeset với nhau tuỳ.

-> JSON Batch tương tự chỉ là truyền mảng vào thôi
Request:
[
  {
    "id": "1",
    "method": "GET",
    "path": "/users/1"
  },
  {
    "id": "2",
    "method": "POST",
    "path": "/orders",
    "body": {
      "productId": 10
    }
  }
]
=> Muốn atomic tự truyền thêm trường "atomic": true là xong.
Response client tự parse lấy data:
--batchresponse_456
Content-Type: application/http
Content-Transfer-Encoding: binary
Content-ID: 1

HTTP/1.1 200 OK
Content-Type: application/json

{
  "id": 1,
  "name": "Alice"
}

--batchresponse_456
Content-Type: application/http
Content-Transfer-Encoding: binary
Content-ID: 2

HTTP/1.1 404 Not Found
Content-Type: application/json

{
  "error": "User not found"
}

--batchresponse_456--



# Fuzzy search alg
Thuật toán tìm kiếm gần đúng trên 1 list. Nhận vào 2 string và trả ra độ so khớp match nhau không.
Để search tự sửa lỗi chính ta, implement thêm spell corrector chấp nhận chuỗi đích có độ so khớp ở 1 mức thấp là được.
Ngoài ra còn có: Levenstein Distance Algorithm, N-gram Algorithm, BK Tree Algorithm, Bitap Algorithm

- Naive Algorithm: 
Duyệt 2 vòng for tìm input xuất hiện trong chuỗi đích trong list thì match. Tức chuỗi tìm kiếm phải liền nhau exactly đúng thứ tự.
- Hamming Distance Algorithm: 
Input là 2 chuỗi cùng kích thước, cho ra số lượng ký tự k khớp nhau. 
Thuật toán giúp tìm gần đúng chuỗi đích nhưng nhược điểm là chuỗi phải cùng độ dài, nếu k cùng độ dài sẽ k luôn k khớp nên kém linh hoạt trong trường hợp ký tự thêm bớt ký tự
- Levenshtein Distance (Edit Distance): 
Đo sự khác biệt giữa hai chuỗi bằng cách đếm số lượng các phép chèn, xóa, thay thế cần thiết để biến chuỗi này thành chuỗi kia.
Thuật toán tốt giúp sủa lỗi chính tả chuẩn

Nếu muốn tìm kiếm một chuỗi ngắn trong một danh sách các chuỗi rất dài sẽ phải duyệt qua từng chuỗi dài và so sánh chuỗi ngắn với các đoạn con của chuỗi dài. Việc này rất chậm và cần tiền xử lý nhiều thứ, khi gặp case này, ta thường dùng các công cụ như Elastic Search chứ k tự implement, Elastic Search có hỗ trợ fuzzy search.
MongoDB có index 'text' hỗ trợ fuzzy search kèm mức độ match
SQL có full text search hỗ trợ search fuzzy chuỗi ngắn trong chuỗi dài, có LIKE tìm đa dạng hơn nhưng chậm, chỉ dùng với tập data nhỏ
SQL có FREETEXT giúp tìm kiếm gần đúng từ đồng nghĩa như google 



# QPS 
Định luật 80/20 là 80% request trong 1 ngày rơi vào 20% thời gian. Nếu hệ thống có 2M request/ngày thì cần bnh QPS và nếu 1 server chịu được 60 QPS thì cần bnh server? 
QPS là số lượng query 1s. Response time là thời gian request đi và về. TPS là transaction per second. VD 1 TPS có 2 QPS tức 1 transaction query tới server 2 lần.
Server của ta cần xử lý được tải lúc giờ cao điểm mới được: 2M request * 80% = 1.6M request
86400 * 20% = 17280s tức 1.6M request rơi vào 20% thời gian là 17200s
QPS = 1.6M request / 17200s = 93 request/s => Cần 2 server hoạt động giờ cao điểm để chạy mượt mà.



# 2 kiểu crawling server
- Server liên tục check và crawl data tĩnh từ 1 nguồn, nếu có data mới thì update vào DB. VD mỗi 24h crawl 1 lần
Nhiều nền tảng họ tạo riêng 1 server để crawl hết các data vào container theo các rules xác định và cung ra API, rồi server sẽ query data trong phạm vi container đó, để tránh crawl data thừa => VD untrest k crawl data ngoài rules

- Server liên tục crawl data từ 1 nguồn, data đó liên tục thêm mới và có dấu thời gian, lượng data rất nhiều và không thể crawl hết 1 lúc. Ta có thể dừng lại và crawl tiếp từ vị trí trước bất cứ lúc nào. Khi crawl đến vị trí mới nhất, server tiếp tục chờ update realtime với data mới.
Đơn giản là lưu cả dấu thời gian vào db, cứ crawl là lấy dấu thời gian đó query tiếp cho tới hiện tại hoặc tới 1 mốc interval tiếp theo. Có cơ chế retry và atomic, đảm bảo dừng và crawl tiếp thoải mái. 
Nhiều nền tảng cho phép subscribe vào 1 nguồn data source là socket để lấy data mới. Phải implement catch error connection break thì tự reconnect và mail thông báo.



# Usecase horizontal scaling theo vị trí địa lý
Nginx có thể xử lý 1M connection đồng thời trên 1 single server. Vậy xử lý 100M request đồng thời thì sao? 
Dùng horizontal scaling mở rộng nhiều server và thêm load balancers. Scale với K8s, Docker. Dùng HTTP3, chống DDoS. Dùng cache, CDN, sharding, socket, message queue. Scale cả vertical. Bảo mật network, máy ảo. Track server load và capacity với monitoring tool. Autoscaling, backup server, thuật toán tự bầu cử tăng tính availability.

Thế nếu dùng load balancer, 100M request vào balancer đó thì toang? 
Dùng nhiều load balancer ở nhiều vị trí địa lý khác nhau. Nếu thực sự lớn thì chia tiếp layered load balancer theo vùng miền. Dùng CDN để giảm tải request tới server.

Có thể tạo 10 load balancer ở các châu lục khác nhau bằng 1 tk aws không? 
Có. Vào EC2 trong từng region tạo server, tạo Elastic Load Balancer trong EC2. Dùng Route 53 định tuyến người dùng từng khu vực vào các load balancer ở các vùng khác nhau. 

Làm sao để cấu hình cho user tự query server hoặc load balancer gần nhất? 
Dùng dịch vụ DNS; Dùng CDN; Ở các load balancer có thể cấu hình thêm các rules để forwards đến server nào khi có nhiều server; Dùng Anycast Routing cho phép 1 IP được gán cho nhiều server và hệ thống mạng tự động định tuyến đến server gần nhất.

Route 53 trong AWS sử dụng pp nào? 
Nó là pp routing dùng dịch vụ DNS Geolocation, có thể cấu hình để gửi cho server gần nhất, hay có độ trễ nhỏ nhất, hay trọng số ưu tiên được set lớn nhất, tuỳ. 

Nhưng nếu server phân tán mà database tập trung thì query vẫn lâu nếu db ở xa? 
Có thể replica database ở nhiều nơi, hoặc dùng db sharding chia ra ở nhiều khu vực như server luôn.
Replica update bị lâu nếu ở xa, nên dùng db sharding hơn. Khi đó data của các nước phải là độc lập nhau. Nó cũng giải quyết vấn đề về chính sách lưu trữ data ở các nước khác nhau có thể khác nhau. Cần thiết kế để query tới 1 nước phụ thuộc vào data chứ k phụ thuộc vào vị trí hiện tại của họ, VD 1 người di chuyển sang nước khác, họ vẫn query shard của nước gốc và chấp nhận query bị lâu hơn. Khi đó có thể tạo thêm cache cho case user di chuyển giữa các nước. 
Cách db sharding này lại k dùng khi cần đồng bộ data realtime các nước. VD tạo bảng xếp hạng toàn cầu.

Nếu replica thì data tự đồng bộ giữa các replica? 
Đúng là nó sẽ tự đồng bộ theo config, có nhiều loại replica khác nhau. 
- Sync replication ghi data đồng thời vào primary và secondary, nhất quán cao nhưng độ trễ cao
- Async replication ghi vào primary trước, secondary sau, sẽ có độ trễ và k nhất quán data
- Semi-sync rep: primary xác nhận 1 vài secondary xong là được
- Multi-master rep: ghi cùng lúc vào các master là coi như xong, k cần quan tâm secondary của các master xong hay chưa.
=> AWS hỗ trợ database có sẵn các tuỳ chọn sao chép data. VD dùng dynamoDB cho multi-master. Tuỳ vào mức độ realtime của data cần như nào.

Dùng replica nhưng 2 master ở 2 khu vực cùng được ghi vào đồng thời sẽ k nhất quán? 
Có thể xảy ra, nếu k cấu hình gì hết, 1 số db mặc định cho bản ghi cuối đè, 1 số db báo lỗi dữ liệu, tệ nhất là khi kết quả cuối cùng là 2 db chứa 2 data khác nhau. 
Nếu thực sự dùng update replica async mà bị xung đột như này, cần phải cấu hình xử lý thêm như versioning, last write wins, user custom rules, ưu tiên request đến từ source nào, lấy random, quorum-based systems, nếu 2 request kèm timestamp mà y hệt nhau thì chọn random chứ nhất quyết k xung đột.
VD trong sql có thể cấu hình SQL Server Merge Replication để xử lý conflict tự động theo rules của ta. Hoặc dùng cloud cung các dịch vụ tự động replica và tự đọng phát hiện xử lý xung đột sẵn mà k cần phải tự cấu hình thủ công.

Trong TH replica 1 master thì chỉ master có thể ghi, các slave chỉ đọc. Always On Availability Groups là 1 tính năng của SQL cho phép tự chuyển đổi dự phòng giữa các bản sao khi có sự cố, đảm bảo bất cứ thời điểm nào cũng luôn có 1 master.



# Backend for Frontend (BFF) Pattern
Là mô hình thiết kế dùng 1 server riêng tối ưu cho FE để dễ call API hơn 
Mỗi loại UI page nên có 1 BFF riêng. VD mobile có thể bỏ đi 1 vài dòng text hay ảnh thì response từ server có thể trim bớt để tối ưu.
Vd web có trang dashboard cần FE gọi 10 API 1 lúc và xử lý sẽ lag thì nên dựng 1 BFF server cung 1 API cho FE gọi, bên trong gọi 10 API (Promise.all) và trả ra kết quả đã xử lý cho FE. FE chỉ cần lo render lên UI.
VD: NextJS client ---Call BFF---> NextJS API ---Call---> 10 API services in BE server.
=> Thực tế nextjs dùng vậy cho tiện SSR, với case khác có thể viết API gom ở ngay server gốc được. Lưu ý chỉ tối ưu khi thực sự gặp vấn đề về performance



# Microservice là gì
Mô hình chia ra ứng dụng lớn thành nhiều module nhỏ, thay vì để nguyên khối. Các mô đun có thể được viết bằng nhiều ngôn ngữ khác nhau và tương tác thông qua API. Vì mỗi mô đun chỉ lo các tính năng riêng nhỏ nên code đơn giản.
Microservice cũng chỉ việc chia ra nhiều server nhỏ lo các chức năng riêng biệt hợp lại với nhau thành 1 hệ thống lớn.

Ngày xưa: Đặt hàng ---> Xử lý [100ms có hàng] ---> Mua hàng
Ngày nay: Đặt hàng --> Xử lý [100ms check giảm giá] [200ms tích điểm] [100ms hội viên] [...] --> Mua hàng
=> Thao tác đặt hàng có thể xử lý mất tới vài giây thay vì tức thì như trước. Do đó các hệ thống lớn tối ưu thông qua microservices, khiến nó trở thành:
Đặt hàng --> Xử lý [100ms] --> Mua hàng
            |  |     |    \ 
            v  v     v      v
      [100ms][200ms][100ms] [...]
Database, cache, messsagequeue là 3 loại storage lưu data buộc phải có trong các hệ thống lớn. VD ActiveMQ, RabbitMQ giúp triển khai mô hình MessageQueue cho microservices



# Các mô hình microservice với API Gateway
API Gateway cũng chỉ là 1 cục trung gian xử lý các thứ, coi là 1 loại reverse proxy thôi. Nó là 1 endpoint duy nhất mà khách hàng giao tiếp, giúp che giấu sự phức tạp của kiến trúc microservices bên dưới.

- Direct Client-To-Microservices: Client App[Mobile, web] ---> [Microservice1 chạy container WebAPI, Microservice2 WebAPI, Microservice3 WebAPI]
Client phải gọi trực tiếp qua API gặp nhiều vấn đề

- 1 API gateway
Mobile ----------------------------------> |             | ---> Microservices WebAPI1
WebApp ----------------------------------> | API Gateway | ---> Microservices WebAPI2
Traditional Web HTML <---> Server MVC _______/^            ---> Microservices WebAPI3

- Multiple API gateway BFF
Mobile ---------------------------> API Gateway Mobile ___> [Microservice1, Microservice2, Microservice3]
WebApp ---------------------------> API Gateway Web ___________/^
Traditional Web <---> Server MVC ______/^      

-> API gateway khác load balancer về phạm vi:
Load balancer chỉ làm công việc phân phối tải khi dịch vụ được duplicate ở nhiều nơi để giảm tải cho 1 server đỡ xử lý quá nhiều request.
API gateway điều hướng request tới các dịch vụ khác nhau. Các dịch vụ này có thể cùng 1 server or ở các server riêng. 
VD có thể để api gateway xác thực, ghi log, bảo mật, cache nữa. Còn server chỉ xử lý logic core bên trong thôi.
VD reverse proxy có thể chặn DDoS nhưng API gateway thường k làm

VD API Gateway: mọi công việc trước khi thực sự xử lý ở các hệ thống riêng đều qua API gateway => ref tới "APIGateway.gif"
Call http request -> API gateway bắt đầu bằng việc validate request -> chạy allow-list/deny-list check chỉ request đúng mới qua -> APIGateway làm việc với identity provider để authen và authorization -> check rate limit, nếu quá rate limit sẽ bị reject -> Dynamic routing thông qua path matching cái request để tìm đúng service thực hiện request -> transform request thành protocol hợp lý và gửi trả cho backend microservice thực hiện -> song song với việc thực thi đó, api gateway cũng check error (qua hệ thống log monitor với ElasticSearch) và tăng tốc với cache data (qua redis).

--> Trong hệ thống lớn, khi nhiều service độc lập gọi lẫn nhau, mỗi log đến từ 1 request phải có cùng 1 traceId. TraceId duy nhất của 1 request giúp debug dễ hơn và duy trì dù gọi qua nhiều server khác nhau. 
Implment khi nhận 1 request và ghi log sẽ lấy traceid được lưu trong request đó nếu k có mới tạo 1 Guid mới.

-> Tạo API gateway bằng nodejs
Có package npm express-gateway giúp chạy gateway cho server express nodejs luôn
Config file yaml để phân phối request của người dùng đến các url xử lý dịch vụ khác nhau



#*** Xử lý phân tán cache thì query vào đâu
Dùng distributed cache thì k sao, nhưng nếu nhiều api server và mỗi server dùng 1 cache riêng thì client query 1 server cache lưu có data rồi nhưng request sau query server khác không có phải get lại dẫn đến 1 data lưu ở nhiều server. 

Giải pháp là dùng pp Hashing hoặc Sticky Sessions thay cho Round Robin, Least Connections. 
Normal hashing sẽ hash thông tin client để lấy ra 1 server khiến cho mọi request của client luôn tới 1 server duy nhất. Nhưng dùng Hashing gặp vấn đề khi server list biến động, lúc nào client cũng gọi vào server đó kể cả khi nó sập hay bị xoá rồi dẫn đến lỗi. Do đó nên dùng:

- Consistent Hashing: 
Dùng 1 hàm hash an toàn cho id của node và id của data record. Ra 1 giá trị thì nhét vào 1 vòng tròn. Mỗi key sẽ gắn với node gần nhất kề nó theo chiều kim. Vd:
0 ---- A ---- key1 ---- B ---- key2 ---- E ---- key3 ---- C ---- key4 ---- D ---- 2^32-1
Key 1 gắn node B, key 2 gắn node E. Nếu ngừoi dùng muốn query data nằm trong khoảng key 4 thì họ query vào cache server D.
VD hash 32bit: 0 ---------------------- 2^32 - 1
Vòng tròn 0 -----1000(A)-----2500(key1)-----3000(B)-----6500(key2)-----7000(C)-----10000(0) khi thêm xoá node thì dịch chuyển data rất dễ vì luôn chỉ cần dịch 2 node.

Các node có thể phân bố data không đều nếu chỉ hash id của server: A -------------------- B -- C -------- D
Giải pháp là hash(server, index) ngẫu nhiên lưu vào metadata db và chọn ra các khoảng đều trên vòng tròn, nhưng vẫn bị lệch khi thêm bớt server nhiều lần. Giải pháp là virtual node.
VD: 
A = máy mạnh
B = máy trung bình
C = máy yếu
thì chia:
A : 200 vnode
B : 100 vnode
C : 50 vnode
Chia so le server A chứa A1 A2 A3 ta sinh hàng loạt token và chọn token so le:
A1 B1 C1 A2 C2 B2 C3 A3 B3
Khi thêm D thì D cắt 1 ít từ nhiều node nhỏ giảm 1 lượng, còn lại vẫn cân bằng:
A1 B1 D1 C1 A2 D2 C2 B2 C3 A3 D3 B3
=> Lưu ý chỉ có ích với kiểu db key-value. Nếu query condition phức tạp, k ai dùng consistent hashing để chia cả mà có db partition.

- Rendezvous Hashing: 
Muốn tìm node nào lưu record hiện tại, ta tính weight của data với từng node: weight(key, node) = hash(key of record + node_id) => Node có giá trị cao nhất sẽ lưu record đó.
=> 2 pp này k những phân tán server mà còn phân tán data. Thực tế chỉ cần check nếu 1 server sập thì request vào fallback server tiếp theo cũng được.

-> Nhược điểm của dedicated server là khó horizontal scaling, việc mua tự lắp đặt thêm server rất mệt.
Nếu thuê VPS thì rất dễ vì nó làm hộ việc scale. Các bước tối thiểu phải xử lý là: dựng 2 servers, cấu hình DNS trỏ tới từng server, xử lý fail over để 1 server sập sẽ đổ traffic vào server còn lại.



# Vấn đề load balancer
Load balancer phân phối tải mà lại có 1 endpoint thì các request ở xa load balancer vẫn bị lâu như thường => Thực tế là đang hiểu sai vai trò của load balancer. Nếu chia ra vài khu vực trên thế giới, môi khu có 1 server thì chả cần load balancer làm gì. Nó chỉ dùng khi 1 khu vực có nhiều server và cần route trong khu vực đó, sau khi horizontal scaling khu vực đó để nó chịu tải tốt hơn. 
Còn muốn sharding mỗi khu vực 1 cụm server và routing giữa các khu vực phải tự implement 1 server duy nhất lưu trạng thái user id có data trong cụm server nào. Routing server này phải phân bố nhiều nơi khắp thế giới để 1 user sang vùng khác vẫn routing về đúng vùng của họ. Có thể dùng cloudflare proxy vì nó phân phối toàn cầu và có sẵn cache.
DNS-based routing k liên quan là pp route các server giữa các vùng khác nhau dựa trên IP, người dùng có thể dùng VPN để fake vùng nên k dựa vào DNS để check.

-> Nếu chỉ dùng 1 db duy nhất (ở sing) thì mọi request ở Mỹ cũng cần gọi vào db ở sing để lấy bị lâu. 
- Read replica ở các vùng => chỉ dùng khi ghi ít vì ở xa sẽ lâu khi update. 
- Multi-master replica đọc ghi đều nhanh nhưng cấu hình phức tạp. Phải có cơ chế fail over, cơ chế đồng bộ database, xử lý conflict. VD dùng safe writes để đảm bảo dữ liệu ghi được xác nhận từ ít nhất 2 node trước khi commit, hoặc nhiều thuật toán đa số khác.
- Sharding data đảm bảo mọi user ở Mỹ sẽ kbh liên can đến data ở Sing. Chấp nhận case user đi từ Mỹ qua Sing thì query vẫn về Mỹ và bị lâu
Có thể dùng database proxy để điều hướng tới db gần nhất, két hợp cache.
=> Thường công ty lớn thường sharding data độc lập giữa customer chứ multi-master rất ít khi dùng. Vì đọc ghi nhanh từ mọi nơi trên thế giới mà update nhiều dễ conflict thì rất hiếm.
- Các dự án nhỏ thường dùng db server cache chung 1 VPS cho nhanh, khi có nhiều người mói scale lên.



# Notification system
Usecase: Giải quyết vấn đề 1M shop tạo ra sp mới, 1 số shop có 1M follower, mỗi sp mới sẽ notify cho tất cả followers.
Mỗi user có 1 bảng user.notification lưu thông báo hiện lên UI. 
Phải dùng MQ để k mất dữ liệu, tốc độ cao, k duplicate dữ liệu, đúng thứ tự.
Phải phân chia đảm bảo tốc độ push notification đạt tối đa khi có nhiều user:
Shop -> new product -> MessageQueue 1 -> User id từ 0 tới 1000
                    -> MessageQueue 2 -> User id từ 1000 tới 2000
Phân tích: Shop -> new product -> system.center.notification -> MQ -> push vào user.notification của từng users

Push: Mỗi notification của 1 shop trong sys.center.notification lấy ra lưu vào 1M user.notification
Pull: khi user login mới pull từ sys.center.notification vào user.notification, pull định kỳ khi online chịu delay 1 xíu.
Shop có 1M follower mà push notification thì bị nặng. Có thể tạo 1 biến flag, mặc định là push notification, shop nào > 100K follower thì switch qua pull notification.
Về business: Trong 1 triệu user, user nào thường xuyên online hoặc đang online thì sẽ push notification tới họ trước. User nào online ít thì khi nào online, họ phải tự vào notification center mà pull về. Riêng trường hợp mã giảm giá hay khách hàng thân thiét, khách hàng đăng ký nhận khuyến mãi, sẽ phải push tới user ngay.

Tại sao lại dùng 1 crawljob lấy data từ db của system.center.notification gửi tới MQ mà không gửi mẹ từ shop tới MQ luôn? 
Vì cần tính toán tới các case khi crash hệ thống. Trong trường hợp này cần đảm bảo khi 1 shop gửi notification tới user thành công, chắc chắn notification đó k bị mất khi crash hệ thống, nếu crash thì phải lưu là chưa gửi hoặc gửi k thành công. Bởi vì việc gửi tới 1 triệu user phải làm từ từ, không thể chờ gửi hết 1 triệu mới báo thành công được. Do đó thành công ở đây là lưu thành công vào system.center.notification để server rảnh làm việc khác, sau đó crawljob mới lấy từ db ra gửi vào MQ. 
Phải có cơ chế retry khi hệ thống khôi phục crash và chạy tiếp từ chỗ cũ gửi cho các user còn lại.

Việc phân phối trực tiếp từ system.center.notification tới database của user, so với gửi vào MQ và MQ gửi tới db 1 triệu user có điểm lợi gì?
MQ cung khả năng đảm bảo tin nhắn không bị mất. Ví dụ đang chạy for loop duyệt 1 triệu user để update db, chẳng may sập server phát sẽ mất. Để đảm bảo thì mỗi lần gửi ta lại phải update database từng user isSent=true, khởi động lại hệ thống sẽ gửi tiếp cho các user isSent=false thì rất phức tạp. Còn MQ kiểm soát tự động hết.
MQ tùy biến có thể thêm worker để xử lý tin nhắn từ queue mà không cần thay đổi cơ sở hạ tầng của MQ. Kiểm soát số lượng tin nhắn worker xử lý song song, VD nếu CPU tốn quá thì giảm đi 1 chút để hoạt động trơn tru. Tách biệt quá trình tạo thông báo và gửi thông báo. Nếu server crash, MQ vẫn hoạt động bth.
Cái crawljob ở đây cũng chỉ việc server subscribe vào queue với 1 số lượng song song bị giới hạn.



# Bài toán xây dựng hệ thống AI optimize storage
Usecase: server detect các file lâu không dùng, file duplicate, file rác .cache và user có thể hỏi trực tiếp về bộ nhớ và server recommend các action để chọn.

Có nhiều kiểu xây ứng dụng AI:
- Hệ thống thủ công dựa trên thuật toán tự implement như minimax hay heuristic. Tự tìm file và phương án tối ưu dựa trên code thuần túy. VD AI chơi cờ.
Còn AI sẽ dùng vào các mục đích như học từ hành vi người dùng và đề xuất các option cá nhân hóa, các file thường xóa, option thường dùng (vừa dùng vừa train). Hoặc đọc nội dung và dán nhãn các file junk, file quan trọng, file duplicate.
- Dùng model AI thực sự. Nó khác thuật toán ở chỗ agent có thể đưa ra cảm quan của riêng nó dựa vào model nó được train.
Có thể tự train model với owned dataset, dùng AI train sẵn. Nhiều API có sẵn cứ input là cho ra output luôn.

Luồng cơ bản của hệ thống build theo pattern Agentic AI:
- Dựa vào input cho AI tìm ra mục đích của user muốn xóa storage, đưa ra gợi ý, general question hay gì. Thu thập data từ db.
Rào trước tất cả hành động liên quan đến storage mà user có thể hỏi.
Dùng AI làm mọi thứ kể cả hỏi storage cost hay đưa ra gợi ý.
- Nếu k set up AI available hoặc catch fail thì fallback về tự build response, k thì trả về response default hoặc lỗi.
- Nếu setup AI, cần tự build request và parse response: System prompt + RAG context + userinput + các message trước -> JSON response
System prompt bảo agent trả về resopnse duy nhất dạng JSON như nào thì parse y dạng đó. Response parse từ { đầu tiên đến } cuối cùng. System prompt có thể viết ở file md riêng và có template.
RAG context là dữ liệu bên ngoài hoặc data trong db mà ta cung cấp để AI dùng làm dữ liệu nền. Khác với system prompt là hướng dẫn cách trả lời.
Các message trước trong đoạn hội thoại có thể config lấy 10 hoặc tùy.
- AI có thể đưa ra action gợi ý và trigger action đó. VD trên window có senmatic kernel, AI có thể đưa ra tên hàm và arguments để call tới 1 API xử lý, chờ response.

Luồng chung:
- Nhập yêu cầu từ người dùng
- AI phân loại intent (ý định người dùng)
- Dựa vào intent để gọi các service khác nhau
- AI xử lý logic phức tạp, ra quyết định, gọi tools
- Trả về kết quả
Thực tế system prompt file có template
BE có thể xử lý stream data thinking process và UI dùng event bắt lần lượt từng chunk của response show ra



# Các service hệ thống lớn
- Metadata service:
Có thể CRUD metadata, gán cho object, filter sort object theo metadata.
Metadata có nhiều loại như: text, department của user, lookup metadata khác, yes no, link address, time.
- Job queue: thường dùng hệ thống bên thứ 3 như RabbitM, topic name lưu vào file config
- Timer, schedule job:
Timer luôn chạy, mỗi khi deploy mới restart lại thôi. Timer lo việc upgrade db, data upgrade, bắt các job và chạy job liên tục, loop retention data. Cơ chế job chạy như nào dựa vào bảng Schedule lưu cron expression. 
- Worker: 
Worker subscribe vào queue và chạy multithread or multiprocess để thực hiện task bất đồng bộ
- Workflow: 
Thực hiện định kỳ bao lâu 1 lần, thực hiện action gì thay mặt user nào, gửi mail cho ai khi run, lồng điều kiện if else phức tạp, các action thực hiện liên tục và có ảnh hưởng đến nhau.
Thực hiện action khi có sự kiện gì trigger thì lưu vào db và trigger hàm tương ứng.



